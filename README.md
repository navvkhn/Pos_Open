# Superscale POS

Multi-tenant SaaS POS built with:
- Streamlit
- Supabase (PostgreSQL)
- SQLAlchemy

## Run locally
1. Create Supabase project
2. Add DATABASE_URL to .env
3. pip install -r requirements.txt
4. streamlit run app.py
